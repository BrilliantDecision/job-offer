const InputSubmit = ({val}) => {
    return (
        <input type="submit" value={val} className="bg-blue-500 text-white border-0 rounded-lg my-5 p-3 w-full cursor-pointer"/>
    )
}

export default InputSubmit;
